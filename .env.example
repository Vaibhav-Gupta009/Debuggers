PORT=5000
JWT_SECRET=change_me_in_production
DB_FILE=./data/app.sqlite
FRONTEND_ORIGIN=http://localhost:5500

# Email (OTP)
# In development, if these are not set, OTP is logged to backend console.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=

